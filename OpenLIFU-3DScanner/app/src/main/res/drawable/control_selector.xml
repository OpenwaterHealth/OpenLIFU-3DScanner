<!-- res/drawable/left_capsule_selector.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- pressed state (optional) -->
    <item android:state_pressed="true" android:drawable="@drawable/control"/>
    <!-- selected state -->
    <item android:state_selected="true" android:drawable="@drawable/control"/>
    <!-- default (NOT selected) -->
    <item android:drawable="@drawable/control_disabled"/>
</selector>